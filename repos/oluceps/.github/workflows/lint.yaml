jobs:
  check:
    name: format check
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Nix
        uses: cachix/install-nix-action@v19

      - name: flake check
        run: |
          nix flake check
        shell: bash

name: lint
on: 
  pull_request:
    branches: ["*"]
  push:
    branches: ["main", "trival"]
          

