when:
  - event: pull_request
    branch: main

steps:
  - name: Verify flake shows content
    image: nixos/nix:2.32.3
    commands: |
      set -euo pipefail

      nix --version
      echo "experimental-features = nix-command flakes" >> /etc/nix/nix.conf
      nix flake show --impure
