name: "Update flake.lock"
on:
  schedule:
    # Run weekly on Sundays at 04:17 UTC
    - cron: '17 4 * * 0'
  workflow_dispatch:

jobs:
  update-flake:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - name: Install nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: |
          experimental-features = nix-command flakes
          access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}

    - name: Configure git
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

    - name: Update flake.lock
      run: nix flake update

    # Gate the push on evaluation: forcing every package's drvPath against the
    # freshly locked nixpkgs catches removed/renamed nixpkgs APIs before the
    # broken lock lands on main. Building is left to build.yml.
    - name: Check evaluation
      run: |
        nix eval --raw .#packages.x86_64-linux \
          --apply 'ps: toString (map (p: p.drvPath) (builtins.attrValues ps))'

    - name: Commit and push if changed
      run: |
        git add flake.lock
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "flake.lock: update [automated]"
          git push
        fi
