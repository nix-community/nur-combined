--- a/vanilla.js
+++ b/vanilla.js
@@ -4,6 +4,7 @@
 
 const instance = new Application({
   ROOT_PATH: __dirname,
+  RUNTIME_PATH: process.env.WALINE_RUNTIME_PATH || path.join(require('node:os').tmpdir(), 'waline'),
   APP_PATH: path.join(__dirname, 'src'),
   proxy: true, // use proxy
   env: 'production',
