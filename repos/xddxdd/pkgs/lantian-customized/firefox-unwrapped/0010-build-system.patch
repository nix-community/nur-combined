diff --git a/python/mozbuild/mozbuild/backend/recursivemake.py b/python/mozbuild/mozbuild/backend/recursivemake.py
index fa4d2b57b1..3185bdb3a5 100644
--- a/python/mozbuild/mozbuild/backend/recursivemake.py
+++ b/python/mozbuild/mozbuild/backend/recursivemake.py
@@ -1563,7 +1563,10 @@ class RecursiveMakeBackend(MakeBackend):
         if isinstance(obj, (Program, SharedLibrary)):
             obj_target = self._pretty_path(obj.output_path, backend_file)
 
-        objs_ref = " \\\n    ".join(os.path.relpath(o, obj.objdir) for o in objs)
+        # Stealthfox fix: use absolute paths for obj dependencies. mozmake 4.4.1
+        # Win32 fails on relative paths > ~150 chars (libwebrtc deeply nested).
+        # Absolute paths bypass the bug; linker accepts them either way.
+        objs_ref = " \\\n    ".join(o for o in objs)
         if isinstance(obj, (SimpleProgram, Program, SharedLibrary)):
             # Don't bother with a list file if we're only linking objects built
             # in this directory.
diff --git a/python/mozbuild/mozbuild/frontend/data.py b/python/mozbuild/mozbuild/frontend/data.py
index cb0492f67c..89a783b294 100644
--- a/python/mozbuild/mozbuild/frontend/data.py
+++ b/python/mozbuild/mozbuild/frontend/data.py
@@ -1174,8 +1174,13 @@ class UnifiedSources(BaseSources):
             # On Windows, path names have a maximum length of 255 characters,
             # so avoid creating extremely long path names.
             unified_prefix = context.relsrcdir
-            if len(unified_prefix) > 20:
-                unified_prefix = unified_prefix[-20:].split("/", 1)[-1]
+            if len(unified_prefix) > 30:
+                # Stealthfox fix: Mozilla's `[-20:]` truncation causes collisions
+                # in FF 150 libwebrtc (e.g. goog_cc_scream_network_controller_gn
+                # and scream_network_controller_gn both → "etwork_controller_gn").
+                # `[-30:]` gives unique prefixes without lengthening filenames
+                # past mozmake's path-handling threshold (~150 chars relative).
+                unified_prefix = unified_prefix[-30:].split("/", 1)[-1]
             unified_prefix = unified_prefix.replace("/", "_")
 
             suffix = self.canonical_suffix[1:]
diff --git a/python/mozbuild/mozbuild/jar.py b/python/mozbuild/mozbuild/jar.py
index ffc4d2d5b3..42c5c2441a 100644
--- a/python/mozbuild/mozbuild/jar.py
+++ b/python/mozbuild/mozbuild/jar.py
@@ -563,6 +563,12 @@ class JarMaker:
             except OSError as e:
                 if e.errno != errno.ENOENT:
                     raise
+            # Stealthfox fix: webcompat builtin-addons CSS files have
+            # very long names that overflow Windows MAX_PATH=260 unless
+            # we use \\?\<absolute> long-path syntax. abspath collapses
+            # the .. so the resolved absolute is much shorter.
+            if os.name == "nt":
+                out = "\\\\?\\" + os.path.abspath(out).replace("/", "\\")
             if "b" in mode:
                 return open(out, mode)
             else:
diff --git a/third_party/python/vsdownload/vsdownload.py b/third_party/python/vsdownload/vsdownload.py
old mode 100755
new mode 100644
index 28f2c660cf..ad86c2cda1
--- a/third_party/python/vsdownload/vsdownload.py
+++ b/third_party/python/vsdownload/vsdownload.py
@@ -24,6 +24,7 @@ import json
 import platform
 import re
 import shutil
+import signal
 import socket
 import subprocess
 import sys
@@ -634,7 +635,27 @@ def _downloadPayload(payload, destname, fileid, allowHashMismatch):
             if "size" in payload:
                 size = payload["size"]
             print("Downloading %s (%s)" % (fileid, formatSize(size)), flush=True)
-            urllib.request.urlretrieve(payload["url"], destname)
+            # Stealthfox: total per-file watchdog. socket.setdefaulttimeout only
+            # catches a fully stalled connection, not a slow trickle from a flaky
+            # CDN edge -- the Win11 SDK "X64 Debuggers And Tools" MSI dripped for
+            # hours without ever erroring, so this 5-attempt loop never retried
+            # and the cross-compile build hung. Cap each file and raise on
+            # overrun so the loop reconnects (fresh CDN edge) instead of hanging.
+            def _dl_watchdog(signum, frame):
+                raise TimeoutError("download exceeded 120s for %s" % fileid)
+            # signal.SIGALRM is Unix-only (the watchdog is for the Linux cross-build).
+            # On native Windows it does not exist, so skip the alarm and rely on the
+            # socket.setdefaulttimeout set in main() instead of crashing get_vs.
+            _have_alarm = hasattr(signal, "SIGALRM")
+            if _have_alarm:
+                _prev_alarm = signal.signal(signal.SIGALRM, _dl_watchdog)
+                signal.alarm(120)
+            try:
+                urllib.request.urlretrieve(payload["url"], destname)
+            finally:
+                if _have_alarm:
+                    signal.alarm(0)
+                    signal.signal(signal.SIGALRM, _prev_alarm)
             if "sha256" in payload:
                 if sha256File(destname).lower() != payload["sha256"].lower():
                     if allowHashMismatch:
