--- a/plugin/pkg/proxy/connect.go
+++ b/plugin/pkg/proxy/connect.go
@@ -116,7 +116,7 @@
 	return &persistConn{c: conn, created: time.Now()}, false, err
 }
 
-func (p *Proxy) lookupDNS(_ctx context.Context, state request.Request, opts Options) (*dns.Msg, net.Addr, string, error) {
+func (p *Proxy) lookupDNS(_ctx context.Context, state request.Request, opts Options) (*dns.Msg, []dns.RR, net.Addr, string, error) {
 	var proto string
 	switch {
 	case opts.ForceTCP: // TCP flag has precedence over UDP flag
@@ -129,7 +129,7 @@
 
 	pc, cached, err := p.transport.Dial(proto)
 	if err != nil {
-		return nil, nil, proto, err
+		return nil, nil, nil, proto, err
 	}
 
 	// Dial may have upgraded the transport (e.g. from udp to tcp for DoT),
@@ -150,6 +150,54 @@
 	// Set buffer size correctly for this client.
 	pc.c.UDPSize = max(uint16(state.Size()), 512) // #nosec G115 -- UDP size fits in uint16
 
+	var retRRs []dns.RR
+
+	if state.QType() == dns.TypeAXFR || state.QType() == dns.TypeIXFR {
+		pc.c.SetWriteDeadline(time.Now().Add(maxTimeout))
+		if err := pc.c.WriteMsg(state.Req); err != nil {
+			pc.c.Close() // not giving it back
+			if err == io.EOF && cached {
+				return nil, nil, localAddr, proto, ErrCachedClosed
+			}
+			return nil, nil, localAddr, proto, err
+		}
+		first := true
+		for {
+			pc.c.SetReadDeadline(time.Now().Add(p.readTimeout))
+			in, err := pc.c.ReadMsg()
+			if err != nil {
+				pc.c.Close() // not giving it back
+				if err == io.EOF && cached {
+					return nil, nil, localAddr, proto, ErrCachedClosed
+				}
+				return nil, retRRs, localAddr, proto, err
+			}
+			if state.Req.Id != in.Id {
+				// out-of-order response. unexpected.
+				continue
+			}
+			if first {
+				if len(in.Answer) == 0 || in.Answer[0].Header().Rrtype != dns.TypeSOA {
+					pc.c.Close()
+					return nil, nil, localAddr, proto, dns.ErrSoa
+				}
+				first = !first
+				if len(in.Answer) == 1 {
+					retRRs = append(retRRs, in.Answer[0])
+					continue
+				}
+			}
+			for _, rr := range in.Answer {
+				retRRs = append(retRRs, rr)
+			}
+			if len(in.Answer) >= 0 && in.Answer[len(in.Answer)-1].Header().Rrtype == dns.TypeSOA {
+				break
+			}
+		}
+		p.transport.Yield(pc)
+		return nil, retRRs, localAddr, proto, nil
+	}
+
 	pc.c.SetWriteDeadline(time.Now().Add(maxTimeout))
 	// records the origin Id before upstream.
 	originId := state.Req.Id
@@ -161,9 +209,9 @@
 	if err := pc.c.WriteMsg(state.Req); err != nil {
 		pc.c.Close() // not giving it back
 		if err == io.EOF && cached {
-			return nil, localAddr, proto, ErrCachedClosed
+			return nil, nil, localAddr, proto, ErrCachedClosed
 		}
-		return nil, localAddr, proto, err
+		return nil, nil, localAddr, proto, err
 	}
 
 	var ret *dns.Msg
@@ -185,13 +233,13 @@
 
 			pc.c.Close() // not giving it back
 			if err == io.EOF && cached {
-				return nil, localAddr, proto, ErrCachedClosed
+				return nil, nil, localAddr, proto, ErrCachedClosed
 			}
 			// recovery the origin Id after upstream.
 			if ret != nil {
 				ret.Id = originId
 			}
-			return ret, localAddr, proto, err
+			return ret, nil, localAddr, proto, err
 		}
 		// drop out-of-order responses
 		if state.Req.Id == ret.Id {
@@ -200,10 +248,10 @@
 	}
 	p.transport.Yield(pc)
 
-	return ret, localAddr, proto, nil
+	return ret, nil, localAddr, proto, nil
 }
 
-func (p *Proxy) lookupDoH(ctx context.Context, state request.Request, _ Options) (*dns.Msg, net.Addr, string, error) {
+func (p *Proxy) lookupDoH(ctx context.Context, state request.Request, _ Options) (*dns.Msg, []dns.RR, net.Addr, string, error) {
 	// DoH always runs over TCP (HTTPS), regardless of the downstream
 	// client's protocol.
 	const proto = "tcp"
@@ -218,47 +266,48 @@
 
 	req, err := doh.NewRequestWithContext(ctx, p.dohMethod, p.addr, state.Req)
 	if err != nil {
-		return nil, nil, proto, err
+		return nil, nil, nil, proto, err
 	}
 
 	resp, err := p.transport.httpClient.Do(req)
 	if err != nil {
-		return nil, localAddr, proto, err
+		return nil, nil, localAddr, proto, err
 	}
 
 	// ResponseToMsg always closes the body via defer resp.Body.Close().
 	ret, err := doh.ResponseToMsg(resp)
 	if err != nil {
-		return nil, localAddr, proto, err
+		return nil, nil, localAddr, proto, err
 	}
 
-	return ret, localAddr, proto, nil
+	return ret, nil, localAddr, proto, nil
 }
 
 // Connect selects an upstream, sends the request and waits for a response. It
 // also returns CoreDNS's own outbound address on the upstream socket
 // (localAddr) and the transport proto ("udp" or "tcp") actually used to reach
 // the upstream.
-func (p *Proxy) Connect(ctx context.Context, state request.Request, opts Options) (*dns.Msg, net.Addr, string, error) {
+func (p *Proxy) Connect(ctx context.Context, state request.Request, opts Options) (*dns.Msg, []dns.RR, net.Addr, string, error) {
 	start := time.Now()
 	originId := state.Req.Id
 
 	var (
 		ret       *dns.Msg
+		records   []dns.RR
 		localAddr net.Addr
 		proto     string
 		err       error
 	)
 	switch p.protocol {
 	case transport.HTTPS:
-		ret, localAddr, proto, err = p.lookupDoH(ctx, state, opts)
+		ret, records, localAddr, proto, err = p.lookupDoH(ctx, state, opts)
 	case transport.DNS, transport.TLS:
-		ret, localAddr, proto, err = p.lookupDNS(ctx, state, opts)
+		ret, records, localAddr, proto, err = p.lookupDNS(ctx, state, opts)
 	default:
-		return nil, nil, "", fmt.Errorf("transport %s not supported to proxy", p.protocol)
+		return nil, nil, nil, "", fmt.Errorf("transport %s not supported to proxy", p.protocol)
 	}
 	if err != nil {
-		return nil, localAddr, proto, err
+		return nil, nil, localAddr, proto, err
 	}
 
 	// recovery the origin Id after upstream.
@@ -271,7 +320,7 @@
 
 	requestDuration.WithLabelValues(p.proxyName, p.addr, rc).Observe(time.Since(start).Seconds())
 
-	return ret, localAddr, proto, nil
+	return ret, records, localAddr, proto, nil
 }
 
 const cumulativeAvgWeight = 4
--- a/plugin/forward/forward.go
+++ b/plugin/forward/forward.go
@@ -173,6 +173,7 @@
 
 		var (
 			ret           *dns.Msg
+			records       []dns.RR
 			localAddr     net.Addr
 			upstreamProto string
 			err           error
@@ -180,7 +181,7 @@
 		opts := f.opts
 
 		for {
-			ret, localAddr, upstreamProto, err = proxy.Connect(ctx, state, opts)
+			ret, records, localAddr, upstreamProto, err = proxy.Connect(ctx, state, opts)
 
 			if err == proxyPkg.ErrCachedClosed { // Remote side closed conn, can only happen with TCP.
 				continue
@@ -193,6 +194,26 @@
 			break
 		}
 
+		if records != nil {
+			ch := make(chan *dns.Envelope)
+			defer close(ch)
+			tr := new(dns.Transfer)
+			go tr.Out(w, r, ch)
+			j, l := 0, 0
+			for i, rr := range records {
+				l += dns.Len(rr)
+				if l > 65000 {
+					ch <- &dns.Envelope{RR: records[j:i]}
+					l = 0
+					j = i
+				}
+			}
+			if j < len(records) {
+				ch <- &dns.Envelope{RR: records[j:]}
+			}
+			return 0, nil
+		}
+
 		if child != nil {
 			child.Finish()
 		}
--- a/plugin/pkg/proxy/proxy_test.go
+++ b/plugin/pkg/proxy/proxy_test.go
@@ -36,7 +36,7 @@
 	rec := dnstest.NewRecorder(&test.ResponseWriter{})
 	req := request.Request{Req: m, W: rec}
 
-	resp, _, _, err := p.Connect(context.Background(), req, Options{PreferUDP: true})
+	resp, _, _, _, err := p.Connect(context.Background(), req, Options{PreferUDP: true})
 	if err != nil {
 		t.Errorf("Failed to connect to testdnsserver: %s", err)
 	}
@@ -67,7 +67,7 @@
 	rec := dnstest.NewRecorder(&test.ResponseWriter{})
 	req := request.Request{Req: m, W: rec}
 
-	_, _, _, err := p.Connect(context.Background(), req, Options{})
+	_, _, _, _, err := p.Connect(context.Background(), req, Options{})
 	if err == nil {
 		t.Fatal("Expected *not* to receive reply, but got one")
 	}
@@ -121,7 +121,7 @@
 				Req: m,
 			}
 
-			resp, _, proto, err := p.Connect(context.Background(), req, tc.opts)
+			resp, _, _, proto, err := p.Connect(context.Background(), req, tc.opts)
 			if err != nil {
 				t.Fatalf("Connect failed: %v", err)
 			}
@@ -218,7 +218,7 @@
 		recorder := dnstest.NewRecorder(&test.ResponseWriter{})
 		request := request.Request{Req: queryMsg, W: recorder}
 
-		response, _, _, err := p.Connect(context.Background(), request, options)
+		response, _, _, _, err := p.Connect(context.Background(), request, options)
 		if err != nil {
 			t.Errorf("Failed to connect to testdnsserver: %s", err)
 		}
