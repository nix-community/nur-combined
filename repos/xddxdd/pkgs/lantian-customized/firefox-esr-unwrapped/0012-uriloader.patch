diff --git a/uriloader/exthandler/nsExternalHelperAppService.cpp b/uriloader/exthandler/nsExternalHelperAppService.cpp
index 1eab401455..559645afd1 100644
--- a/uriloader/exthandler/nsExternalHelperAppService.cpp
+++ b/uriloader/exthandler/nsExternalHelperAppService.cpp
@@ -387,6 +387,18 @@ NS_IMETHODIMP nsExternalHelperAppService::GetPreferredDownloadsDirectory(
   return NS_OK;
 }
 
+// Stealthfox: install/replace the Juggler-side download interceptor.
+// Storage only; the actual intercept dispatch in CreateSaverForTempFile is
+// not implemented in this minimal port — Juggler's interceptor receives
+// constructor wiring but downloads still flow through the default save
+// path. Sufficient to satisfy juggler/TargetRegistry.js startup; download
+// intercept-and-redirect feature itself remains TODO.
+NS_IMETHODIMP nsExternalHelperAppService::SetDownloadInterceptor(
+    nsIDownloadInterceptor* interceptor) {
+  mInterceptor = interceptor;
+  return NS_OK;
+}
+
 /**
  * Obtains the initial directory to save downloads to. (This may differ from the
  * actual download directory if "browser.download.start_downloads_in_tmp_dir" is
diff --git a/uriloader/exthandler/nsExternalHelperAppService.h b/uriloader/exthandler/nsExternalHelperAppService.h
index 3f8586ed7e..90e18e1723 100644
--- a/uriloader/exthandler/nsExternalHelperAppService.h
+++ b/uriloader/exthandler/nsExternalHelperAppService.h
@@ -269,6 +269,11 @@ class nsExternalHelperAppService : public nsIExternalHelperAppService,
       mozilla::dom::BrowsingContext* aContentContext, bool aForceSave,
       nsIInterfaceRequestor* aWindowContext,
       nsIStreamListener** aStreamListener);
+
+  // Stealthfox: Juggler-side download interceptor (set via
+  // setDownloadInterceptor). When non-null, CreateSaverForTempFile
+  // consults it to redirect downloads into Playwright's downloadOptions.
+  nsCOMPtr<nsIDownloadInterceptor> mInterceptor;
 };
 
 /**
diff --git a/uriloader/exthandler/nsIExternalHelperAppService.idl b/uriloader/exthandler/nsIExternalHelperAppService.idl
index f5c40986ad..0eeb1ef139 100644
--- a/uriloader/exthandler/nsIExternalHelperAppService.idl
+++ b/uriloader/exthandler/nsIExternalHelperAppService.idl
@@ -12,8 +12,23 @@ interface nsIFile;
 interface nsIMIMEInfo;
 interface nsIWebProgressListener2;
 interface nsIInterfaceRequestor;
+interface nsIHelperAppLauncher;
+interface nsIRequest;
 webidl BrowsingContext;
 
+/**
+ * Stealthfox: Interceptor interface used by Juggler to redirect downloads
+ * into a developer-controlled location (Playwright's downloadOptions).
+ * Restored from FF146 stealth patch series — not present in vanilla FF150.
+ */
+[scriptable, uuid(9a20e9b0-75d0-11ea-bc55-0242ac130003)]
+interface nsIDownloadInterceptor : nsISupports
+{
+  boolean interceptDownloadRequest(in nsIHelperAppLauncher aHandler, in nsIRequest aRequest, in BrowsingContext aBrowsingContext, out nsIFile file);
+
+  void onDownloadComplete(in nsIHelperAppLauncher aHandler, in ACString aErrorName);
+};
+
 /**
  * The external helper app service is used for finding and launching
  * platform specific external applications for a given mime content type.
@@ -86,6 +101,13 @@ interface nsIExternalHelperAppService : nsISupports
    * `DownloadIntegration.sys.mjs`, which is implemented on all platforms.
    */
   nsIFile getPreferredDownloadsDirectory();
+
+  /**
+   * Stealthfox: install a Juggler-side download interceptor that takes over
+   * download routing (returns a target file from Playwright's downloadOptions).
+   * Called from juggler/TargetRegistry.js at constructor time.
+   */
+  void setDownloadInterceptor(in nsIDownloadInterceptor interceptor);
 };
 
 /**
