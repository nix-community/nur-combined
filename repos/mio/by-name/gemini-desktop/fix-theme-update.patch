diff --git a/src/main/managers/ipc/ThemeIpcHandler.ts b/src/main/managers/ipc/ThemeIpcHandler.ts
index 8c271aa..a4bfd8b 100644
--- a/src/main/managers/ipc/ThemeIpcHandler.ts
+++ b/src/main/managers/ipc/ThemeIpcHandler.ts
@@ -47,6 +47,14 @@ export class ThemeIpcHandler extends BaseIpcHandler {
             const savedTheme = this.deps.store.get('theme') || 'system';
             nativeTheme.themeSource = savedTheme;
             this.logger.log(`Native theme initialized to: ${savedTheme}`);
+
+            // Listen for OS theme changes
+            nativeTheme.on('updated', () => {
+                const currentPreference = this.deps.store.get('theme') || 'system';
+                const effectiveTheme = nativeTheme.shouldUseDarkColors ? 'dark' : 'light';
+                this.logger.log(`OS theme updated. New effective theme: ${effectiveTheme}`);
+                this._broadcastThemeChange(currentPreference, effectiveTheme);
+            });
         } catch (error) {
             this.handleError('initializing native theme', error);
         }
