--- a/src/api-impl-jni/graphics/android_graphics_BitmapFactory.c	2026-08-09 16:50:37.593321259 +1000
+++ b/src/api-impl-jni/graphics/android_graphics_BitmapFactory.c	2026-08-09 16:50:37.600321217 +1000
@@ -58,8 +58,18 @@
 {
 	GInputStream *stream = java_input_stream_new(env, is, storage);
 
-	GdkPixbuf *pixbuf = gdk_pixbuf_new_from_stream(stream, NULL, NULL);
+	GError *error = NULL;
+	GdkPixbuf *pixbuf = gdk_pixbuf_new_from_stream(stream, NULL, &error);
 	g_object_unref(stream);
+
+	if (!pixbuf) {
+		if (error) {
+			g_warning("gdk_pixbuf_new_from_stream failed: %s", error->message);
+			g_error_free(error);
+		}
+		return 0;
+	}
+
 	GdkTexture *texture = gdk_texture_new_for_pixbuf(pixbuf);
 	g_object_unref(pixbuf);
 	return _INTPTR(texture);
