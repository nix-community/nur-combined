builds:
  exclude:
    - packages.*
    # - legacyPackages.*
    - nixosConfigurations.bootstrap
  include:
    - '*.x86_64-linux.*'
    - checks.*
    - homeConfigurations.*
    - nixosConfigurations.*
