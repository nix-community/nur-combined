builds:
  exclude:
    - "checks.*.*"
    - "packages.*.*"
    - "legacyPackages.*.*"
    - "nixosConfigurations.bootstrap"
  include:
    # - "devShells.*.*"
    - '*.x86_64-linux.*'
    - '*.aarch64-linux.*'
    - "homeConfigurations.*"
    - "nixosConfigurations.*"
