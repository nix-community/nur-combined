name: "Build and populate cache"
# Forgejo-native port of .github/workflows/build.yml.
# Runs on the nas runner (host executor), so nix is already installed and
# builds share the system store. The GitHub-only automation in the original
# (update-flake-lock PRs, auto-format PRs, gh api calls) intentionally stays
# on GitHub Actions.
on:
  pull_request:
  push:
    branches:
      - main
      - master
  schedule:
    # rebuild everyday at 8:26
    - cron: "26 8 * * *"
  workflow_dispatch:
jobs:
  build:
    runs-on: native
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Show nixpkgs version
        run: nix-instantiate --eval -E '(builtins.getFlake (builtins.toString ./.)).inputs.nixos-unstable.lib.version'
      - name: Check evaluation
        run: nix flake show
      - name: Check formatting
        run: nix fmt -- --fail-on-change
      - name: Build nix packages
        run: |
          nix run nixpkgs#cachix -- watch-store toyvo &
          nix build --print-build-logs ".#checks.$(nix eval --raw --impure --expr builtins.currentSystem).all" --option accept-flake-config true
        env:
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
  nur:
    needs: build
    strategy:
      matrix:
        nixPath:
          - nixpkgs=https://github.com/NixOS/nixpkgs/archive/refs/heads/nixpkgs-unstable.tar.gz
          - nixpkgs=https://github.com/NixOS/nixpkgs/archive/refs/heads/nixos-unstable.tar.gz
          - nixpkgs=https://github.com/NixOS/nixpkgs/archive/refs/heads/nixos-26.05.tar.gz
    runs-on: native
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Show nixpkgs version
        run: NIX_PATH="${{ matrix.nixPath }}" nix-instantiate --eval -E '(import <nixpkgs> {}).lib.version'
      - name: Check evaluation
        run: |
          NIX_PATH="${{ matrix.nixPath }}" nix-env -f . -qa \* --meta --xml \
            --allowed-uris https://static.rust-lang.org \
            --option restrict-eval true \
            --option allow-import-from-derivation true \
            --drv-path --show-trace \
            -I nixpkgs=$(NIX_PATH="${{ matrix.nixPath }}" nix-instantiate --find-file nixpkgs) \
            -I $PWD
      - name: Build nix packages
        run: |
          nix run nixpkgs#cachix -- watch-store toyvo &
          NIX_PATH="${{ matrix.nixPath }}" nix shell -f '<nixpkgs>' nix-build-uncached -c nix-build-uncached ci.nix -A cacheOutputs
        env:
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Trigger NUR update
        if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
        run: curl -XPOST "https://nur-update.nix-community.org/update?repo=https://github.com/ToyVo/nixcfg"
