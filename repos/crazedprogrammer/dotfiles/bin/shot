#!/usr/bin/env bash

path=$HOME/Pictures/Screenshots/$(date +"%Y%m%d-%H%M%S").png

case $1 in
	"s") args="--noopengl -s" ;;
	"u") args="-i $(xdotool getactivewindow)" ;;
	"m") args="" ;;
	*) exit 1 ;;
esac

maim $args $path

upload $path
