name: update

on:
  schedule:
    - cron: "0 9 * * *" # every day at 09:00 UTC
  workflow_dispatch:

concurrency:
  group: update

permissions:
  contents: read
  id-token: write

jobs:
  pkgs:
    strategy:
      fail-fast: false
      matrix:
        image:
          - ubuntu-24.04
          - ubuntu-24.04-arm
          - macos-26
    runs-on: ${{ matrix.image }}
    steps:
      - name: Initialize
        uses: spotdemo4/nix-init@404b7e941df54f8cde2a4efb20c2affa819af909 # v1.30.0
        with:
          app_id: ${{ vars.CLIENT_ID }}
          app_key: ${{ secrets.PRIVATE_KEY }}
          shell: update

      - name: Create branch
        run: git checkout -B "update-packages-${{ matrix.image }}"

      - name: Update packages
        run: update

      - name: Push changes
        run: git push --force origin "update-packages-${{ matrix.image }}"

  pkgs-pr:
    needs: pkgs
    runs-on: ubuntu-latest
    steps:
      - id: checkout
        name: Checkout
        uses: spotdemo4/better-checkout@48475b2dda4ef6945cb56e846429b43799121549 # v0.5.1
        with:
          app-id: ${{ vars.CLIENT_ID }}
          private-key: ${{ secrets.PRIVATE_KEY }}
          fetch-depth: 0

      - name: Create branch
        run: |
          git checkout -B update-packages
          git merge -X ours --no-ff origin/update-packages-ubuntu-24.04
          git merge -X ours --no-ff origin/update-packages-ubuntu-24.04-arm
          git merge -X ours --no-ff origin/update-packages-macos-26
          git push --force origin update-packages

      - id: pr
        name: Create pull request
        run: |
          if url=$(gh pr create --title "package updates" --body "updated some packages" --head update-packages --base main); then
            echo "Created PR: ${url}"
            echo "url=${url}" >> $GITHUB_OUTPUT
          else
            echo "Could not create PR"
          fi
        env:
          GH_TOKEN: ${{ steps.checkout.outputs.token }}

      - if: ${{ steps.pr.outputs.url }}
        name: Enable automerge
        run: gh pr merge --rebase --auto "${{ steps.pr.outputs.url }}"
        env:
          GH_TOKEN: ${{ steps.checkout.outputs.token }}

  renovate:
    runs-on: ubuntu-latest
    steps:
      - id: init
        name: Initialize
        uses: spotdemo4/nix-init@404b7e941df54f8cde2a4efb20c2affa819af909 # v1.30.0
        with:
          app_id: ${{ vars.CLIENT_ID }}
          app_key: ${{ secrets.PRIVATE_KEY }}
          shell: update

      - name: Renovate
        run: renovate
        env:
          RENOVATE_CONFIG_FILE: .github/renovate.json
          RENOVATE_TOKEN: ${{ steps.init.outputs.token }}
          LOG_LEVEL: ${{ runner.debug == '1' && 'debug' || 'info' }}
