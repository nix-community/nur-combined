# `bun2nix` phoenix template

Example of building a [phoenix](https://www.phoenixframework.org/) project using `bun2nix` to compile the frontend.

This also serves as a realistic use of `bun2nix.hook` to integrate with another language's tool-chain.
