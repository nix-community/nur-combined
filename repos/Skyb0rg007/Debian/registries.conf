# Podman resolves bare image names like the `FROM debian:sid` in debcraft's
# Containerfile only against these registries. NixOS ships no default, so
# scripts/buildbin.sh points CONTAINERS_REGISTRIES_CONF at this file.
unqualified-search-registries = ["docker.io"]
