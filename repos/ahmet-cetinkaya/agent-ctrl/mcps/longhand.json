{
  "mcpServers": {
    "longhand": {
      "command": "longhand",
      "args": [
        "mcp-server"
      ],
      "description": "Lossless Claude Code session history — indexes raw tool calls, file edits, and thinking blocks from ~/.claude/projects/*.jsonl into local SQLite + ChromaDB before Claude Code rotates them. Provides verbatim recall of past sessions. Install: pip install longhand && longhand setup"
    }
  }
}
