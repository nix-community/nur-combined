[project]
name = "scriptipy"
version = "0.0.1"
dependencies = [
  "pydantic",
  "requests",
  "humanfriendly",
  "httpx",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["scriptipy"]
