#!/usr/bin/env -S sd nix lang
#!nix-shell -p sanoid -i bash
#! vim:ft=bash

# backup some zfs datasets to the storage zpool

sudo syncoid -r zroot/vms storage/backup/vms
sudo syncoid -r zroot/games storage/games
