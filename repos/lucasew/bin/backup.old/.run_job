#!/usr/bin/env bash

set -euo pipefail

HOST="$1"; shift
LOCATION="$1"; shift
DESTINATION=lucasew@whiterun:/media/storage/backup/hosts

FINAL_DESTINATION="$(echo "$DESTINATION/$HOST/$LOCATION" | sed 's;//;/;g')"

rsync --mkpath -avP "$LOCATION/" "$FINAL_DESTINATION" "$@"
