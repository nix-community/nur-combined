# base16-dunst

Writes `~/.config/dunst/dunstrc` from the shared palette. Needs `base16`. See [../README.md](../README.md) for slots.

Urgency colors: critical `base08`, normal `base0D`, low `base0C`; text `base06`.

Reload: `killall dunst` (it comes back on the next notification), or `dunst &`.
