#!/usr/bin/env bash
set -euo pipefail

if ! command -v modot >/dev/null 2>&1; then
	printf 'mise: modot not found on PATH\n' >&2
	exit 127
fi

# Home lazy tool "mise" (registry:mise). Pins live in the home lockfile.
exec modot open lazy --home --bin mise mise -- "$@"
