name: Update packages

on:
  push:
    branches:
      - main
    paths:
      - 'pkgs/**'
      - 'nvfetcher.toml'
      - '.github/workflows/update.yml'
  schedule:
    - cron: "23 12 * * *"
  workflow_dispatch:

permissions:
  actions: write
  contents: write

concurrency:
  group: update-packages
  cancel-in-progress: false

jobs:
  update:
    name: Update package sources and hashes
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - name: Maximize Nix disk space
      uses: wimpysworld/nothing-but-nix@v10
      with:
        hatchet-protocol: cleave

    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: |
          accept-flake-config = false
          access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
          substituters = https://mzwing.cachix.org https://cache.nixos.org https://cache.nixos-cuda.org https://nix-community.cachix.org https://attic.xuyh0120.win/lantian
          trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= mzwing.cachix.org-1:tOO3NqAwrXyPCnecEl/0wXwparCRksM5TeuS/wZK+KA= cache.nixos-cuda.org:74DUi4Ye579gUqzH4ziL9IyiJBlDpMRn9MBN8oNan9M= nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs= lantian:EeAUQ+W+6r7EtwnmYjeVwx5kOGEBpjlBfPlzGlTNvHc=

    - name: Restore Nix store
      uses: nix-community/cache-nix-action@v7
      with:
        primary-key: nix-update-${{ hashFiles('devenv.lock', 'flake.lock', 'devenv.nix', 'devenv.yaml', 'nvfetcher.toml') }}
        restore-prefixes-first-match: nix-update-
        gc-max-store-size-linux: 4G

    # Builds or substitutions interrupted in earlier runs (e.g. job
    # timeouts) leave root-owned <path>.lock files in /nix/store. They ride
    # along in the restored store cache, and the unprivileged tar of the
    # post-run store save cannot read them ("Cannot open: Permission
    # denied"). No nix process is running at this point, so any lock file
    # present is stale by definition.
    - name: Remove stale Nix store lock files
      run: sudo find /nix/store -maxdepth 1 -name '*.lock' -delete

    - name: Restore nvfetcher database
      uses: actions/cache@v6
      with:
        path: ~/.local/share/nvfetcher
        key: nvfetcher-${{ github.sha }}
        restore-keys: |
          nvfetcher-

    - name: Update devenv inputs
      run: |
        nix profile add nixpkgs#devenv
        devenv update

    - name: Update sources, lockfiles and dependency hashes
      shell: bash
      run: |
        set -euo pipefail
        trap 'rm -f secrets.toml' EXIT

        # nothing-but-nix cleaves almost all disk into the /nix mount
        # (runner-writable) and leaves / with ~1G. The gomod2nix module
        # downloads (and crate2nix's cargo registry) overflow / unless
        # their caches live on the big mount.
        export GOMODCACHE=/nix/var/tmp/gomodcache
        export CARGO_HOME=/nix/var/tmp/cargo
        mkdir -p "$GOMODCACHE" "$CARGO_HOME"

        cat >secrets.toml <<EOF
        [keys]
        github = "${{ secrets.GITHUB_TOKEN }}"
        EOF

        nix run .#update-sources

        nix run .#update-lockfiles

        nix run .#update-hashes

        rm -f secrets.toml
        trap - EXIT

    - name: Commit changes
      id: commit
      shell: bash
      run: |
        set -euo pipefail

        git config user.name 'github-actions[bot]'
        git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
        git add -- devenv.lock flake.lock _sources pkgs

        if git diff --cached --quiet; then
          echo 'changed=false' >>"${GITHUB_OUTPUT}"
          exit 0
        fi

        git commit -m 'chore: update packages'
        echo 'changed=true' >>"${GITHUB_OUTPUT}"

    - name: Push changes
      if: steps.commit.outputs.changed == 'true'
      uses: ad-m/github-push-action@master
      with:
        branch: ${{ github.ref }}

    - name: Trigger distributed package build
      if: steps.commit.outputs.changed == 'true'
      env:
        GH_TOKEN: ${{ github.token }}
      run: gh workflow run build.yml --ref ${{ github.ref }}
