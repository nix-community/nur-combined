name: Build and populate cache

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'pkgs/**'
      - '_sources/**'
      - 'nvfetcher.toml'
      - 'README.md'
      - '.github/workflows/update.yml'
      - '.github/workflows/tests.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nur-packages-${{ github.ref }}
  cancel-in-progress: false

env:
  CACHE_NAME: mzwing
  BUILDER_PREFIX: nur-b-${{ github.run_id }}-${{ github.run_attempt }}
  CACHE_HOST: nur-cache-${{ github.run_id }}-${{ github.run_attempt }}

jobs:
  config:
    name: Plan package builds
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      builders: ${{ steps.plan.outputs.builders }}
      has_builds: ${{ steps.plan.outputs.has_builds }}
      targets: ${{ steps.plan.outputs.targets }}
    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - name: Set up Nix
      uses: mzwing/nix-config/.github/actions/setup-nix@master
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        cachix-name: ${{ env.CACHE_NAME }}

    # Read-only reader of the nix-ci- store cache family written by
    # the daily update.yml run (the only writer — readers must never
    # save: cache-nix-action skips the save on an exact primary-key
    # hit, so the first writer's store would otherwise poison the
    # family). Restores the most recent entry via the prefix: the warm
    # store holds the fetched package sources this job's evaluation
    # needs.
    - name: Restore Nix store
      uses: nix-community/cache-nix-action/restore@v7
      with:
        primary-key: nix-ci-${{ hashFiles('devenv.lock', 'flake.lock', 'devenv.nix', 'devenv.yaml', 'nvfetcher.toml') }}
        restore-prefixes-first-match: nix-ci-

    - name: Evaluate supported packages
      id: evaluate
      shell: bash
      run: |
        set -euo pipefail
        printf 'targets=%s\n' "$(nix eval --json --impure --file ci/targets.nix)" >>"${GITHUB_OUTPUT}"

    - name: Check caches and generate builder matrix
      id: plan
      env:
        ALL_TARGETS: ${{ steps.evaluate.outputs.targets }}
      run: python3 scripts/plan-builds.py

  builder:
    name: Builder ${{ matrix.id }}
    needs: config
    if: needs.config.outputs.has_builds == 'true'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 360
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.config.outputs.builders) }}
    steps:
    # Cachix is pull-only on builders: only final package outputs are
    # pushed there, by the coordinator. Locally built paths (crate2nix
    # crates, goModules FODs, ...) go to the central builder store cache
    # instead (see the "cache" job), which also keeps the Cachix write
    # token off the unsandboxed macOS builders.
    - name: Set up Nix
      uses: mzwing/nix-config/.github/actions/setup-nix@master
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        cachix-name: ${{ env.CACHE_NAME }}

    # The action restores the macOS runner's DNS after joining (with
    # DNS accepted, the tailscale action replaces the system DNS with
    # quad100 and the runner loses public internet access, so it is
    # reset back to DHCP). This also drops MagicDNS, hence the
    # /etc/hosts pin below.
    - name: Join builder network
      uses: mzwing/nix-config/.github/actions/join-ci-tailnet@master
      with:
        oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
        oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
        tags: tag:nur-ci-builder
        hostname: ${{ env.BUILDER_PREFIX }}-${{ matrix.id }}
        args: --ssh

    # macOS only: with MagicDNS gone (see above), the Attic cache hostname
    # would not resolve. Linux builders resolve it via split DNS.
    - name: Resolve builder store cache address
      if: endsWith(matrix.system, '-darwin')
      uses: mzwing/nix-config/.github/actions/pin-tailnet-peer-address@master
      with:
        hostname: ${{ env.CACHE_HOST }}

    # Fetch the run-local Attic token and signing public key only after
    # joining the tailnet. The post-build hook uploads sparse locally built
    # outputs; Attic drops anything signed by a configured public upstream.
    - name: Configure builder store cache
      uses: mzwing/nix-config/.github/actions/setup-builder-store-cache@master
      with:
        cache-host: ${{ env.CACHE_HOST }}

    - name: Wait for distributed builds
      uses: mzwing/nix-config/.github/actions/wait-for-distributed-builds@master

  # Central sparse Attic cache for the builders. Its binary-cache database
  # may reference public-cache paths without storing them, so Actions Cache
  # contains only private/local NARs rather than a complete /nix/store.
  # Reconciliation supplies the current-lock keep-set before shutdown;
  # Attic then deletes stale objects and orphan data deterministically.
  cache:
    name: Builder store cache
    needs: config
    if: needs.config.outputs.has_builds == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 420
    permissions:
      # actions: write is required to purge expired cache entries.
      actions: write
      contents: read
    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - name: Maximize Nix disk space
      uses: wimpysworld/nothing-but-nix@v10
      with:
        hatchet-protocol: cleave

    - name: Set up Nix
      uses: mzwing/nix-config/.github/actions/setup-nix@master
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        cachix-name: ${{ env.CACHE_NAME }}
        # The cache job only substitutes prebuilt Attic tools; never compile
        # packages locally on this orchestration runner.
        max-jobs: '0'

    # With nix:false the cache action extracts as the runner user rather than
    # managing /nix permissions, so prepare its only writable subtree first.
    - name: Prepare Attic data directory
      run: |
        sudo install -d -o "$(id -u)" -g "$(id -g)" \
          /nix/attic-data /nix/attic-cache-tmp /nix/attic-checkpoint
        # Attic blobs are write-once and the SQLite DB is rebuilt every run,
        # so btrfs copy-on-write and data checksumming only amplify writes
        # on the loop-backed pool. Disable them for the Attic subtree.
        sudo chattr +C /nix/attic-data /nix/attic-cache-tmp /nix/attic-checkpoint
        # The runner resolves its temp directory before the job starts and
        # forces that value onto cache action steps, so a per-step env
        # override cannot relocate the multi-GiB tar/zstd staging off the
        # tiny root filesystem (nothing-but-nix preallocates all but ~2G).
        # Replace the runner temp directory with a symlink into the /nix
        # pool; the currently executing step script stays readable through
        # its already-open file descriptor.
        sudo rm -rf "${RUNNER_TEMP}"
        sudo ln -s /nix/attic-cache-tmp "${RUNNER_TEMP}"

    # The unique key prevents save races; prefix restore picks the latest
    # Attic database and NAR storage. `nix: false` is essential: the ordinary
    # Nix store is deliberately excluded from this cache.
    - name: Restore builder store cache
      uses: nix-community/cache-nix-action/restore@v7
      with:
        primary-key: nix-builder-attic-${{ github.run_id }}-${{ github.run_attempt }}
        restore-prefixes-first-match: nix-builder-attic-
        nix: false
        paths: /nix/attic-data

    # A surviving checkpoint entry always postdates the newest finalized
    # generation: checkpoints snapshot the restored generation plus the
    # run's pushes so far, and every successful final save purges the
    # checkpoint family. So whenever one exists, adopt it over the
    # finalized restore.
    - name: Restore checkpoint builder store cache
      uses: nix-community/cache-nix-action/restore@v7
      with:
        primary-key: nix-builder-atticchk-${{ github.run_id }}-${{ github.run_attempt }}
        restore-prefixes-first-match: nix-builder-atticchk-
        nix: false
        paths: /nix/attic-checkpoint

    - name: Adopt checkpoint generation
      run: |
        if [[ -s /nix/attic-checkpoint/server.db ]]; then
          rsync -a /nix/attic-checkpoint/ /nix/attic-data/
          echo 'Adopted a mid-run checkpoint Attic generation.'
        fi
        # /nix itself is root-owned, so only the runner-owned contents are
        # removable; the (re-populated) directory stays for the later save.
        find /nix/attic-checkpoint -mindepth 1 -delete

    - name: Join cache network
      uses: mzwing/nix-config/.github/actions/join-ci-tailnet@master
      with:
        oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
        oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
        tags: tag:nur-ci-cache
        hostname: ${{ env.CACHE_HOST }}
        args: --ssh

    # Starts Attic in the background and returns; the job continues with
    # the mid-run checkpoint snapshot before the finalize step shuts Attic
    # down. Client bootstrap (push-only token, rotated signing key) is
    # published over Tailscale SSH as before.
    - name: Start Attic
      uses: mzwing/nix-config/.github/actions/start-builder-store-cache@master
      with:
        cache-host: ${{ env.CACHE_HOST }}

    # Checkpoint rounds: the coordinator signals right after its Cachix
    # push, but a cold rebuild can run for hours before that. Each round
    # stages a consistent snapshot (online SQLite backup + hardlinked
    # write-once storage) on signal OR timeout, so even a cancelled run
    # leaves a recent warm generation behind. Rounds with no new Attic
    # objects skip staging; each save purges the previous checkpoint entry.
    - name: Wait for checkpoint (round 1)
      id: attic-checkpoint-1
      uses: mzwing/nix-config/.github/actions/stage-builder-store-cache-checkpoint@master
      with:
        timeout-minutes: '45'

    - name: Save builder store cache checkpoint (round 1)
      if: ${{ !cancelled() && steps.attic-checkpoint-1.outputs.checkpoint-ready == 'true' }}
      uses: nix-community/cache-nix-action/save@v7
      with:
        primary-key: nix-builder-atticchk-${{ github.run_id }}-${{ github.run_attempt }}-1
        nix: false
        paths: /nix/attic-checkpoint
        purge: true
        purge-prefixes: nix-builder-atticchk-
        purge-last-accessed: 0
        purge-primary-key: never

    - name: Wait for checkpoint (round 2)
      id: attic-checkpoint-2
      uses: mzwing/nix-config/.github/actions/stage-builder-store-cache-checkpoint@master
      with:
        timeout-minutes: '60'

    - name: Save builder store cache checkpoint (round 2)
      if: ${{ !cancelled() && steps.attic-checkpoint-2.outputs.checkpoint-ready == 'true' }}
      uses: nix-community/cache-nix-action/save@v7
      with:
        primary-key: nix-builder-atticchk-${{ github.run_id }}-${{ github.run_attempt }}-2
        nix: false
        paths: /nix/attic-checkpoint
        purge: true
        purge-prefixes: nix-builder-atticchk-
        purge-last-accessed: 0
        purge-primary-key: never

    - name: Wait for checkpoint (round 3)
      id: attic-checkpoint-3
      uses: mzwing/nix-config/.github/actions/stage-builder-store-cache-checkpoint@master
      with:
        timeout-minutes: '60'

    - name: Save builder store cache checkpoint (round 3)
      if: ${{ !cancelled() && steps.attic-checkpoint-3.outputs.checkpoint-ready == 'true' }}
      uses: nix-community/cache-nix-action/save@v7
      with:
        primary-key: nix-builder-atticchk-${{ github.run_id }}-${{ github.run_attempt }}-3
        nix: false
        paths: /nix/attic-checkpoint
        purge: true
        purge-prefixes: nix-builder-atticchk-
        purge-last-accessed: 0
        purge-primary-key: never

    - name: Wait for checkpoint (round 4)
      id: attic-checkpoint-4
      uses: mzwing/nix-config/.github/actions/stage-builder-store-cache-checkpoint@master
      with:
        timeout-minutes: '60'

    - name: Save builder store cache checkpoint (round 4)
      if: ${{ !cancelled() && steps.attic-checkpoint-4.outputs.checkpoint-ready == 'true' }}
      uses: nix-community/cache-nix-action/save@v7
      with:
        primary-key: nix-builder-atticchk-${{ github.run_id }}-${{ github.run_attempt }}-4
        nix: false
        paths: /nix/attic-checkpoint
        purge: true
        purge-prefixes: nix-builder-atticchk-
        purge-last-accessed: 0
        purge-primary-key: never

    - name: Wait for checkpoint (round 5)
      id: attic-checkpoint-5
      uses: mzwing/nix-config/.github/actions/stage-builder-store-cache-checkpoint@master
      with:
        timeout-minutes: '60'

    - name: Save builder store cache checkpoint (round 5)
      if: ${{ !cancelled() && steps.attic-checkpoint-5.outputs.checkpoint-ready == 'true' }}
      uses: nix-community/cache-nix-action/save@v7
      with:
        primary-key: nix-builder-atticchk-${{ github.run_id }}-${{ github.run_attempt }}-5
        nix: false
        paths: /nix/attic-checkpoint
        purge: true
        purge-prefixes: nix-builder-atticchk-
        purge-last-accessed: 0
        purge-primary-key: never

    - name: Wait for checkpoint (round 6)
      id: attic-checkpoint-6
      uses: mzwing/nix-config/.github/actions/stage-builder-store-cache-checkpoint@master
      with:
        timeout-minutes: '60'

    - name: Save builder store cache checkpoint (round 6)
      if: ${{ !cancelled() && steps.attic-checkpoint-6.outputs.checkpoint-ready == 'true' }}
      uses: nix-community/cache-nix-action/save@v7
      with:
        primary-key: nix-builder-atticchk-${{ github.run_id }}-${{ github.run_attempt }}-6
        nix: false
        paths: /nix/attic-checkpoint
        purge: true
        purge-prefixes: nix-builder-atticchk-
        purge-last-accessed: 0
        purge-primary-key: never

    - name: Finalize Attic
      id: attic
      uses: mzwing/nix-config/.github/actions/finalize-builder-store-cache@master
      with:
        cache-host: ${{ env.CACHE_HOST }}

    # Save even on a red build: successfully uploaded crate/FOD paths remain
    # useful. Save only Attic's sparse data directory, keep this family at
    # exactly one entry, and purge legacy full-store entries during migration.
    - name: Save builder store cache
      if: ${{ !cancelled() && steps.attic.outputs.save-ready == 'true' }}
      uses: nix-community/cache-nix-action/save@v7
      with:
        primary-key: nix-builder-attic-${{ github.run_id }}-${{ github.run_attempt }}
        nix: false
        paths: /nix/attic-data
        purge: true
        purge-prefixes: |-
          nix-builder-attic-
          nix-builder-atticchk-
          nix-builder-store-
        purge-last-accessed: 0
        purge-primary-key: never

  coordinator:
    name: Distributed build coordinator
    needs: config
    if: needs.config.outputs.has_builds == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - name: Maximize Nix disk space
      if: needs.config.outputs.has_builds == 'true'
      uses: wimpysworld/nothing-but-nix@v10
      with:
        hatchet-protocol: cleave

    - name: Set up Nix
      uses: mzwing/nix-config/.github/actions/setup-nix@master
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        cachix-name: ${{ env.CACHE_NAME }}
        max-jobs: '0'

    - name: Join coordinator network
      if: needs.config.outputs.has_builds == 'true'
      uses: mzwing/nix-config/.github/actions/join-ci-tailnet@master
      with:
        oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
        oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
        tags: tag:nur-ci-coordinator
        hostname: nur-c-${{ github.run_id }}-${{ github.run_attempt }}

    # The coordinator substitutes locally built runtime dependencies
    # from the builder store cache when assembling the Cachix push
    # closure (they are not uploaded to Cachix by the builders). Runs
    # before the nix-daemon restart in "Configure remote builders" so
    # the daemon picks up the new substituter.
    - name: Configure builder store cache substituter
      uses: mzwing/nix-config/.github/actions/setup-builder-store-cache@master
      with:
        cache-host: ${{ env.CACHE_HOST }}
        post-build-hook: 'false'

    - name: Configure remote builders
      if: needs.config.outputs.has_builds == 'true'
      uses: mzwing/nix-config/.github/actions/setup-remote-builders@master
      with:
        builders-json: ${{ needs.config.outputs.builders }}
        builder-prefix: ${{ env.BUILDER_PREFIX }}
        ssh-host-pattern: "nur-b-* nur-cache-*"

    # The Attic job joins the tailnet in parallel; make sure its HTTP
    # binary-cache endpoint works both directly and through a builder
    # before Nix gets a chance to memoize a transient failure.
    - name: Check builder store cache reachability
      uses: mzwing/nix-config/.github/actions/check-builder-store-cache-reachability@master
      with:
        cache-host: ${{ env.CACHE_HOST }}
        builders-json: ${{ needs.config.outputs.builders }}
        builder-prefix: ${{ env.BUILDER_PREFIX }}

    - name: Build package outputs
      if: needs.config.outputs.targets != '[]'
      env:
        TARGETS: ${{ needs.config.outputs.targets }}
      shell: bash
      run: |
        set -euo pipefail
        build_systems="$(jq -c '[.[].system] | unique' <<<"${TARGETS}")"
        set +e
        {
          printf '### Built package outputs\n\n'
          BUILD_SYSTEMS="${build_systems}" nix build \
            --impure \
            --file ci/outputs.nix \
            --no-link \
            --no-write-lock-file \
            --print-build-logs \
            --print-out-paths \
            --max-jobs 0 \
            --keep-going |
            tee /tmp/built-outputs.txt |
            while IFS= read -r path; do
              printf -- "- \`%s\`\n" "${path}"
            done
        } >>"${GITHUB_STEP_SUMMARY}"
        build_status=$?
        set -e
        # `nix build` prints out-paths only when the whole invocation
        # succeeds; with --keep-going one failing package would hide every
        # other realised output from the Cachix push set. Re-derive the
        # realised outputs directly from the local store instead.
        : > /tmp/built-outputs.txt
        while IFS= read -r out; do
          if nix-store --check-validity "${out}" 2>/dev/null; then
            printf '%s\n' "${out}" >> /tmp/built-outputs.txt
          fi
        done < <(jq -r '.[].outputPath' <<<"${TARGETS}" | sort --unique)
        exit "${build_status}"

    # Only the 'out' outputs of final packages are pushed to Cachix;
    # everything else (crate2nix crates, goModules FODs, ...) lives in
    # the builder store cache. Auxiliary outputs like crate2nix's '-lib'
    # must NOT be pushed: their nix-support/propagated-build-inputs and
    # lib/link files reference the entire crate graph, and `cachix push`
    # would upload that whole build-time closure as "runtime
    # dependencies", bloating the public cache with intermediates.
    # Thanks to --keep-going the build step also produces the succeeded
    # outputs when other packages fail, so this runs on red builds too
    # (!cancelled) and publishes the partial result.
    - name: Assemble Cachix push set
      if: '!cancelled()'
      env:
        TARGETS: ${{ needs.config.outputs.targets }}
      shell: bash
      run: |
        set -euo pipefail
        jq -r '.[].drvPath' <<<"${TARGETS}" \
          | sort --unique > /tmp/active-drvs.txt
        jq -r '.[] | select(.outputName == "out") | .outputPath' <<<"${TARGETS}" \
          > /tmp/out-targets.txt
        grep '^/nix/store/' /tmp/built-outputs.txt 2>/dev/null \
          | grep -Fxf /tmp/out-targets.txt > /tmp/push-outputs.txt || true

    - name: Push final outputs to Cachix
      if: '!cancelled()'
      uses: mzwing/nix-config/.github/actions/push-store-closure-to-cachix@master
      env:
        CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
      with:
        cachix-name: ${{ env.CACHE_NAME }}
        paths-file: /tmp/push-outputs.txt
        closure-paths-file: /tmp/cachix-published-closure.txt

    # Lets the cache job snapshot a consistent mid-run Attic generation
    # before reconciliation, so even a cancelled run leaves a warm cache.
    - name: Signal builder store cache checkpoint
      if: '!cancelled()'
      run: |
        ssh -n "root@${{ env.CACHE_HOST }}" 'touch /tmp/cache_checkpoint' \
          || echo '::warning::Could not signal the Attic checkpoint; continuing without it.'

    # Post-build uploads are best-effort. Re-offer every output in the exact
    # scheduled derivation closures while builders are alive; Attic performs a bulk
    # missing check, skips public-upstream signatures, and receives the
    # same expected set as its deterministic shutdown keep-list.
    - name: Reconcile builder store cache
      if: '!cancelled()'
      uses: mzwing/nix-config/.github/actions/reconcile-builder-store-cache@master
      with:
        builders-json: ${{ needs.config.outputs.builders }}
        builder-prefix: ${{ env.BUILDER_PREFIX }}
        cache-host: ${{ env.CACHE_HOST }}
        active-drvs-file: /tmp/active-drvs.txt
        # Written atomically only after Cachix accepted the full closure.
        public-paths-file: /tmp/cachix-published-closure.txt

    # Also signals the builder store cache machine that the run is over
    # (/tmp/cache_done, via the cache-host input).
    - name: Release remote builders
      if: always() && needs.config.outputs.has_builds == 'true'
      uses: mzwing/nix-config/.github/actions/release-remote-builders@master
      with:
        builders-json: ${{ needs.config.outputs.builders }}
        builder-prefix: ${{ env.BUILDER_PREFIX }}
        cache-host: ${{ env.CACHE_HOST }}

    - name: Trigger NUR update
      if: success()
      run: curl -XPOST "https://nur-update.nix-community.org/update?repo=mzwing"
