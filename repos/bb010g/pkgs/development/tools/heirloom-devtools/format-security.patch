--- a/make/vroot/report.cc
+++ b/make/vroot/report.cc
@@ -235,7 +235,7 @@
 		fprintf(command_output_fp, "%s:", sfile);
 	}
 	fprintf(command_output_fp, " ");
-	fprintf(command_output_fp, iflag);
+	fprintf(command_output_fp, "%s", iflag);
 	if (iflag != NULL) {
 		is_path = 1;
 	}
