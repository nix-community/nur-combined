--- a/src/rimeaction.cpp
+++ b/src/rimeaction.cpp
@@ -69,14 +69,6 @@
     return stringutils::concat(disabledText_, " → ", enabledText_);
 }
 
-std::optional<std::string> ToggleAction::snapshotOption(InputContext *ic) {
-    auto value = optionValue(engine_, ic, /*requestSession=*/false, option_);
-    if (!value.has_value()) {
-        return std::nullopt;
-    }
-    return *value ? option_ : stringutils::concat("!", option_);
-}
-
 bool ToggleAction::checkOptionName(std::string_view name) const {
     return name == option_;
 }
@@ -134,24 +126,6 @@
     return texts_[0];
 }
 
-std::optional<std::string> SelectAction::snapshotOption(InputContext *ic) {
-    auto *state = engine_->state(ic);
-    auto *api = engine_->api();
-    if (!state) {
-        return std::nullopt;
-    }
-    auto session = state->session(false);
-    if (!session) {
-        return std::nullopt;
-    }
-    for (auto &option : options_) {
-        if (api->get_option(session, option.c_str())) {
-            return option;
-        }
-    }
-    return std::nullopt;
-}
-
 bool SelectAction::checkOptionName(std::string_view name) const {
     return std::find(options_.begin(), options_.end(), name) != options_.end();
 }
--- a/src/rimeaction.h
+++ b/src/rimeaction.h
@@ -21,8 +21,6 @@
 
 class RimeOptionAction : public Action {
 public:
-    // This is used to save the option when we need to release the session.
-    virtual std::optional<std::string> snapshotOption(InputContext *ic) = 0;
     // Return the label of current option.
     virtual std::string optionLabel(InputContext *ic) = 0;
     // Check whether a option name belongs to the action.
@@ -41,8 +39,6 @@
 
     std::string icon(InputContext * /*unused*/) const override { return ""; }
 
-    std::optional<std::string> snapshotOption(InputContext *ic) override;
-
     const std::string &option() const { return option_; }
 
     std::string optionLabel(InputContext *ic) override;
@@ -66,8 +62,6 @@
 
     std::string icon(InputContext * /*unused*/) const override { return ""; }
 
-    std::optional<std::string> snapshotOption(InputContext *ic) override;
-
     const std::vector<std::string> &options() const { return options_; }
 
     std::string optionLabel(InputContext *ic) override;
--- a/src/rimeengine.cpp
+++ b/src/rimeengine.cpp
@@ -657,6 +657,9 @@
                 continue;
             }
             std::string optionName = name;
+            // The state of every schema switch must survive a session
+            // release, no matter whether it also gets a status area action.
+            optionNames_[schema].push_back(optionName);
             if (optionName == RIME_ASCII_MODE) {
                 // imAction_ has latin mode that does the same
                 continue;
@@ -670,6 +673,8 @@
             if (labels.size() != options.size()) {
                 continue;
             }
+            optionNames_[schema].insert(optionNames_[schema].end(),
+                                        options.begin(), options.end());
             optionActions_[schema].emplace_back(
                 std::make_unique<SelectAction>(this, schema, options, labels));
         }
@@ -681,6 +686,7 @@
     schemas_.clear();
     schemActions_.clear();
     optionActions_.clear();
+    optionNames_.clear();
     RimeSchemaList list;
     list.size = 0;
     if (api_->get_schema_list(&list)) {
--- a/src/rimeengine.h
+++ b/src/rimeengine.h
@@ -42,6 +42,7 @@
 #include <thread>
 #include <unordered_map>
 #include <unordered_set>
+#include <vector>
 
 #ifndef FCITX_RIME_NO_DBUS
 #include "rimeservice.h"
@@ -162,6 +163,7 @@
     void allowNotification(std::string type = "");
     const auto &schemas() const { return schemas_; }
     const auto &optionActions() const { return optionActions_; };
+    const auto &optionNames() const { return optionNames_; }
 
 private:
     static void rimeNotificationHandler(void *context, RimeSessionId session,
@@ -213,6 +215,7 @@
     std::unordered_map<std::string,
                        std::list<std::unique_ptr<RimeOptionAction>>>
         optionActions_;
+    std::unordered_map<std::string, std::vector<std::string>> optionNames_;
     Menu schemaMenu_;
     std::unique_ptr<HandlerTableEntry<EventHandler>> globalConfigReloadHandle_;
 
--- a/src/rimestate.cpp
+++ b/src/rimestate.cpp
@@ -499,15 +499,24 @@
     if (schema.empty()) {
         return {};
     }
-    std::vector<std::string> savedOptions;
-    const auto &optionActions = engine_->optionActions();
-    auto iter = optionActions.find(schema);
-    if (iter == optionActions.end()) {
+    const auto &optionNames = engine_->optionNames();
+    auto iter = optionNames.find(schema);
+    if (iter == optionNames.end()) {
+        return {};
+    }
+    auto *api = engine_->api();
+    auto session = this->session(false);
+    if (!session) {
         return {};
     }
+    std::vector<std::string> savedOptions;
+    savedOptions.reserve(iter->second.size());
     for (const auto &option : iter->second) {
-        if (auto savedOption = option->snapshotOption(&ic_)) {
-            savedOptions.push_back(std::move(*savedOption));
+        // "!" prefix means the option is off, see restore().
+        if (api->get_option(session, option.c_str())) {
+            savedOptions.push_back(option);
+        } else {
+            savedOptions.push_back(stringutils::concat("!", option));
         }
     }
     return savedOptions;
